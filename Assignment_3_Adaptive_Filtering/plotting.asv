function plotting(datasets, titles, Fs, time)

figure;
len = length(datasets)
%x_axis = 0:(1/Fs):time;

for i = 1:len
    subplot(1, 3, i);
    plot(datasets(i));
    title(titles(i));
    xlabel("Time (s)");
    ylabel("Amplitude (mV)");
end
    

end